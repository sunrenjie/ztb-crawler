
=============
Requirements:
crawler: see /requirements.txt.
sender: Windows XP with VBScript runtime, AutoItX3.dll (registered with
  regsvr32), GNU sleep (within PATH), online robot QQ account with client
  windows open and not minimized.

Notes:
1. Windows XP is required because it is the tested Windows version that
ensures VBScript interoperability with AutoItX3.dll.

